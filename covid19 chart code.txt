  const barChart = (
    data.confirmed ? (
      <Bar data = {()} />
    ) : null
  )	

title: {display: true, text: `Current state in ${country}`}

  const barChart = (
    data.confirmed ? (
      <Bar data = {{}} 
      options={{
        legend: {display: false}, 
        
    }} 
    />
    ) : null
  )